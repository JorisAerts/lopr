import type { UUID } from '../../shared/UUID'

export const useCache = (options: {}) => {
  //
  const getIds = (): UUID[] => {
    return []
  }
  //
  const getResponseData = (uuid: UUID) => {
    //
  }

  return { getIds }
}
