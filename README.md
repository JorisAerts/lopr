# Reverse Proxy

A JavaScript reverse proxy implementation

To build the proxy server:

```shell
yarn build:node
```

To build the client:

```shell
yarn build:client
```

To run the (built) proxy server:

```shell
yarn run:node
```
